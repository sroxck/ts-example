"use strict";
function join(first, second) {
    return "" + first + second;
}
join('sroxck', '@jdd.com');
function join1(first, second) {
    return "" + first + second;
}
join1('sroxck', 2);
function sgg(par) {
    return par;
}
sgg(['3', '4']);
function sgg1(par) {
    return par;
}
sgg1(['3', '4']);
function fx(one, two) {
    return "" + one + two;
}
fx('22', 3);
