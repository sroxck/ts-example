const pe: string = "33334443"
//sfsfs

  // "include": ["tsts.ts"],// 编译包含的文件 或者目录
  // "files": ["tsts.ts"],// 编译包含的文件,类似include 只能是文件,不会被exclude排除
  // "exclude": ["tsts.ts"], // 编译除了包含的文件之外的所有文件
    //  "strict": true,    // 严格模式,代码规范 ,为true 下面的配置项均为true 不可配置 为false 可单独配置
    // "noImplicitAny": true,  //允许注解类型any不用特意标明
    // "strictNullChecks": true, // 允许null值出现在其他类别中                   
    // "strictFunctionTypes": true,                
    // "strictBindCallApply": true,               
    // "strictPropertyInitialization": true,      
    // "noImplicitThis": true,        
    // "alwaysStrict": true,  
    
    // noImplicitAny为true 类型为any必须标明 否则报错
    // rootDir 入口文件,从哪个文件夹打包
    // outDir 输出文件,输出到哪个文件夹
    // "noUnusedLocals": true,       没使用过的变量不进行打包                
    // "noUnusedParameters": true,                 没用过的方法不进行打包

    // Source Map 生产环境对应的文件映射
// https://typescript.bootcss.com/compiler-options.html 可查看完整的配置
function faggh(name:any){
    return name
}